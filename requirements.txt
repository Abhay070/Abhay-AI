# Praxis — the app. Nothing here is a model library; the brain lives behind
# an HTTP call (or in scratch/, if you point PROVIDER at your own model).
fastapi>=0.110
uvicorn>=0.27
httpx>=0.27
pydantic>=2.0
python-multipart>=0.0.9   # file uploads

# Optional extras:
#   pypdf        read PDFs you upload
#   anthropic    the Claude backend (PROVIDER=anthropic)
#   torch        the scratch backend (PROVIDER=scratch)
